version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    healthcheck:
      test: ["CMD", "node", "-e", "require('./index.js'); console.log('ok')"]
      interval: 30s
      timeout: 10s
      retries: 3
