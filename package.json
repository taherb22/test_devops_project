{
  "name": "simple-node-app",
  "version": "0.1.0",
  "description": "Minimal Express app with /metrics for Prometheus",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "NODE_ENV=development node index.js",
    "lint": "eslint . --ext .js",
    "test": "echo \"No tests defined\" && exit 0"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "express": "^4.18.2",
    "prom-client": "^14.0.1"
  },
  "devDependencies": {
    "eslint": "^8.40.0"
  }
}
